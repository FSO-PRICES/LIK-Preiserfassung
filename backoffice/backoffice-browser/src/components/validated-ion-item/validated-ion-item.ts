import { Component } from '@angular/core';
import { FormControl } from '@angular/forms';

@Component({
    selector: 'ion-item[validated]',
    templateUrl: 'validated-ion-item.html'
})
export class ValidatedIonItemComponent {
    // @ViewChild(FormControl) inputComponent: FormControl;
}
